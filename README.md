This project provides a slider widget and a scale selector widget created using react js that can be used as react components.

### Steps ###
* Clone the repo
* run npm install
* run npm start
* to run tests, run npm run test

